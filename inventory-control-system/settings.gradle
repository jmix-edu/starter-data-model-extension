rootProject.name = 'inventory-control-system-addon'

include 'inventory-control-system'
include 'inventory-control-system-starter'

rootProject.children.each { p1 ->
    p1.buildFileName = "${p1.name}.gradle"
}
